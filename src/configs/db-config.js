const DBConfig = {
    host        : 'localhost',
    database    : 'DAI_TP8',
    user        : 'postgres',
    password    : 'root', //root 4 school
    port        : 5432
}

export default DBConfig;