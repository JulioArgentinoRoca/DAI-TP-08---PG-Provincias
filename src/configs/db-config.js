const DBConfig = {
    host        : 'localhost',
    database    : 'DAI_TP8',
    user        : 'postgres',
    password    : 'iranian dick mashallah', //root 4 school
    port        : 5432
}

export default DBConfig;